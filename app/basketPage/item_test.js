'use strict';

describe('myApp.item module', function() {

  beforeEach(module('myApp.item'));

  describe('item controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});